<template>
    <div class="cf-page-refusal">
        <div class="row justify-content-center">
            <div class="col-12 col-md-6 text-center">
                <Icon name="secure-checkout" width="91" height="104" class="d-block m-auto mb-extra-tight" />

                <slot></slot>

                <h1 class="fw-bold mb-base">Access denied</h1>

                <p class="mb-loose">
                    {{ message }}
                </p>

                <a v-if="includeLink" :href="linkHref" class="btn btn-primary">{{ linkText }}</a>
            </div>
        </div>
    </div>
</template>

<script lang="ts">
    import { defineComponent } from 'vue';

    // components
    import Icon from './Icon';

    const PageRefusal = defineComponent({
        name: "PageRefusal",
        components: {
            Icon,
        },
        props: {
            includeLink: { type: Boolean, default: true },
            linkHref: { type: String, default: 'https://crutchfield.sharepoint.com/' },
            linkText: { type: String, default: `Set link text or don't include link` },
            message: { type: String, default: 'Set refusal message' }
        }
    });

    export default PageRefusal;
</script>