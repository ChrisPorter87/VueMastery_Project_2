type crutchPackOptions = {
    appPath?: string,
    entryfolder?: string,
    outputFolder?: string,
    outputFileExtension?: string,
    useChunksFolder?: boolean,
    watchFiles?: boolean,
    callback?: () => void,
    environmentVariables?: object,
    compilerMode?: 'production' | 'development'
}

type crutchSassOptions = {
    appPath?: string,
    bootStrapPath?: string
    inputPath?: string,
    outputPath?: string,
}

export {
    crutchPackOptions,
    crutchSassOptions
}